# 可选：自定义切图根地址（不含末尾 /assets，代码会自动拼接 /assets/文件名）
# VITE_FIGMA_ASSET_BASE=https://your-cdn.example.com/static

# 扫码「手机浏览」跳转的完整 URL（手机无法访问本机 localhost，开发时默认用 GitHub Pages）
# 代码会自动追加 ?embed=1 与 #embed=1（避免 GitHub Pages 重定向丢 query），手机端只显示车展长页
# 同一 WiFi 想用手机打开本机 dev，可填：http://192.168.x.x:5190/
# VITE_QR_PREVIEW_URL=https://shespeggy-bot.github.io/carshow/
